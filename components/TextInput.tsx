import { View, TextInput } from "react-native";
import React from "react";

export default function MainInput() {
	return (
		<View>
			<TextInput className="h-12 w-full rounded-xl border-2 border-gray-900" />
		</View>
	);
}
